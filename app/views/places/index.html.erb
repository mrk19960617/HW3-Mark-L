<h1>Places I've Been</h1>

<ul>
  <% @places.each do |place| %>
    <li><%= link_to place["name"], "/places/#{place["id"]}" %></li>
  <% end %>
</ul>

<p><%= link_to "Add a New Place", "/places/new" %></p>
